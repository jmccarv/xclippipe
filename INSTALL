You must have cmake to build this source

From the root of this repository:

mkdir build
cd build
cmake ..
make
sudo make install

or to install to a different prefix other than /usr/local:
cmake -DCMAKE_INSTALL_PREFIX:PATH=/opt ..
